listeners:
  - name: hy2-in
    type: hysteria2
    listen: 127.0.0.1
    port: 18443
    users:
      alice: "alice-password"
    ignore-client-bandwidth: false
    certificate: ./cert.pem
    private-key: ./key.pem
