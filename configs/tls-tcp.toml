# Plain TLS 1.3 + raw TCP relay — no Vision padding
# Compatible with sing-box/mihomo TLS transport (no flow needed).
listen = "0.0.0.0:443"

[[users]]
id = "12345678-1234-1234-1234-123456789abc"
email = "user@example.com"
flow = ""

[tls]
